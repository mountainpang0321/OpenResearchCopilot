name: paperpilot
description: Analyze academic papers (PDFs) to extract structured research information and automatically generate comprehensive literature reviews.
tools:

exec

以下为 OpenClaw 调用的说明描述

1. User requests paper analysis.

2. OpenClaw uses exec tool to run the python analyzer.

3. Output the structured JSON or Markdown report to the user.